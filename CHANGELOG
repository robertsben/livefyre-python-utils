=== 1.1.3 2014-04-23

* Renaming stream to s_type and adding checks to make sure that type is of a recognized type. Adding legacy stream_type support.

* Fixing url validator. Adding utility class.

* Made building checksum into its method.

* Added get_collection_id method, and added checksum to buildCollectionMetaToken. Changed method from buildLfToken to buildLivefyreToken.

* Needed addn. changes to ensure that python3 is supported.

* Reorganized modules. Added param checks. Added initial Livefyre.get_network static call.

* Initial commit.